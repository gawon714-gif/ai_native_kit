"""AI_Native_Kit — one-command bootstrap for AI-Native Engineering setup.

Copies a curated set of Claude Code assets (TDD/review sub-agent templates,
a branch-scaffolding git hook, reusable PR/review slash commands, and a
CLAUDE.md skeleton) into any project, rendering project-specific values from
an optional ``ai-native-kit.toml`` config.
"""

__version__ = "0.1.0"
